import React from "react";

const StartBox = () => {
  return (
    <form className="d-flex">
      <button className="inputLogIn fw-bold" title="Log In">
        Log In
      </button>
      <button className="inputSignIn fw-bold" title="Sign In">
        Sign In
      </button>
    </form>
  );
};

export default StartBox;
